import styled from "styled-components";

export const Main = styled.main`
  max-width: 1120px;
  margin: 0 auto;

  .container-knowledge-level {
    padding: 2rem 3rem;

    flex: 1;

    header {
      h1 {
        font-size: 1.5rem;
        font-weight: 600;
      }
    }

    p {
      margin: 1rem 0 3rem;
    }
  }
`;
